import { AreAllTagsEnabledPipe } from './are-all-tags-enabled.pipe';

describe('AreAllTagsEnabledPipe', () => {
  it('create an instance', () => {
    const pipe = new AreAllTagsEnabledPipe();
    expect(pipe).toBeTruthy();
  });
});
